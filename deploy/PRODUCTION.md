# Deployment boundary — NOT an operational launch approval

The root Compose file is a LAB development environment. It has not been executed in the implementation sandbox. Never repoint its database volume/credentials to production. API/worker images run as UID 10001. Only loopback ports are mapped; PostgreSQL is on an internal network. An egress network is provided for private R2 API calls. Memory limits target development, not capacity guarantees.

For a separate production deployment:

1. Provision PostgreSQL 17 with independent owner, API and worker credentials. Apply migrations with the owner, then `deploy/grants.sql`. Never run API/worker as the owner or superuser. Revoke `CREATE` on public. Network reachability alone does not grant application access.
2. Set `APP_ENV=production`, exact HTTPS `APP_ORIGIN`, and an independent random `EDGE_SERVICE_SECRET`. Use a secret manager/container environment; do not log or commit credentials. Rotate service secrets jointly on Rust and Workers; there is no insecure fallback.
3. Run the API on loopback on the same host as cloudflared or a dedicated private container network. Do not bind/map its port on public interfaces. The example's `0.0.0.0` is inside Docker; the host mapping is explicitly `127.0.0.1`.
4. Build `crates/edge` with workers-rs/worker-build. The loader generated by the Rust WASM toolchain contains JavaScript glue; all new application logic is Rust. Wrangler itself is a build/deployment tool. Bind `PRIVATE_API` to a verified HTTP VPC Service and set its target port to 8080. Supply `EDGE_SERVICE_SECRET` as a Worker secret. Its runtime Fetcher compatibility is still unverified against an actual VPC account.
5. Route only `studio.audeniq.com/api/*` to this BFF after a separately authorized deployment. Preserve existing Studio static asset routing. Disable workers.dev and previews. BFF rejects other origins/hosts and administrator paths. A VPC error yields failure, never a public-origin fallback.
6. Use a dedicated PRIVATE R2 bucket and a bucket-scoped token. Never enable r2.dev public access. Set `S3_ENDPOINT=https://<account>.r2.cloudflarestorage.com`, `S3_REGION=auto`, bucket/access/secret environment values. Apply exact-origin CORS from `r2-cors.json` after verifying the real domain. Do not allow wildcard credentials origins.
7. Validate presigned PUT's signed content length, MIME and nonce in real browsers and R2. Browsers set Content-Length automatically; client UI should not attempt to set that forbidden header manually. Preserve exact Content-Type and nonce. 512MiB audio and 20MiB artwork are conservative Foundation limits. Multipart is deliberately not offered.
8. Validate HeadObject and conditional CopyObject including embedded-error handling, signature TTL, interrupted uploads, and overwrite of the temporary key after finalization. Final objects use a key never signed for browser writes. `REGISTERED` means object metadata/binding checked, NOT content MIME/QC/SHA approved. ETags are never SHA-256.
9. Cloudflare Access administrator flows remain absent/404. Before adding one, validate Access JWT signature/issuer/audience/expiry plus independent AUDENIQ admin authorization/MFA. Never trust a forwarded email header.
10. Configure backup/PITR, restore drill, R2 independent protection, logging retention, DLQ owners and monitoring. No customer email, DSP submission or payout exists in this version. No RPO/RTO or server-capacity claim has been verified.

Cloudflare references checked during implementation (2026-09-23):
- https://developers.cloudflare.com/workers-vpc/configuration/vpc-services/
- https://developers.cloudflare.com/workers-vpc/api/
- https://developers.cloudflare.com/r2/api/s3/presigned-urls/
- https://developers.cloudflare.com/r2/buckets/cors/

Current configuration examples are not evidence of an established Tunnel, VPC Service, Access policy or R2 account connection.
