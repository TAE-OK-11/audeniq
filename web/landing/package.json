{
  "name": "audeniq-cloudflare-workers",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.4.0",
  "engines": {
    "bun": ">=1.4.0 <1.5.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/worker.js && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.40.0"
  }
}
